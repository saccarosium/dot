#!/usr/bin/env bash

tagger() {
    for target in "$@" 
    do
        track_n=$(echo "$target" | cut -d' ' -f 1)
        if [[ "$track_n" -eq 2 ]]
        then
            echo "Dio cane"
        fi
        
    done
}

tagger "$(ls *.wav | tr ' ' '\n')"
